class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        k=list(set(nums))
        nums.sort()
        k.sort()
        if k==nums:
            return False
        else:
            return True
